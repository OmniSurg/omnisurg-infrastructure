# OmniSurg PRODUCTION deploy stack.
#
# This file is placed on the VPS at /opt/omnisurg/docker-compose.yml by the
# production bootstrap (plan-ab). It is fully ISOLATED from the staging stack
# (deploy/staging) and from miride and pikes: its own Docker network
# (omnisurg_net), its own Postgres and Redis containers, its own Traefik on
# distinct high ports (:8080 plain, :8443 TLS), its own host-bound volumes under
# /opt/omnisurg/data, and its own per-service secret env files under
# /opt/omnisurg/env. See spec 2026-07-13-two-environment-deploy-design section 3.
#
# NO build context: every app service pulls a pinned, PRIVATE ghcr image
# (image-only deploy, spec section 5). The moving production tag defaults to
# :prod; the deploy job overrides IMAGE_TAG with the immutable commit :sha for
# a pinned rollout and rollback.
#
# NO secrets are inline. Non-secret wiring (peer hostnames, ports, OMNISURG_ENV,
# migration table names, CORS origins) is inline as environment:. Every secret
# (database DSN with its per-service password, JWT secret, KEK, Afrosoft key,
# Redis password, R2 credentials, Sentry DSN) is injected at runtime through a
# per-service env_file that carries ONLY that service's own secrets (least
# privilege, spec section 7). The env files are rendered by deploy/render-env.sh.
#
# Only Traefik publishes host ports. Postgres, Redis, and every app service are
# reachable ONLY on the internal network, never from the host (a deploy-posture
# hardening over the local stack, which publishes everything for dev). The BFF
# is reached from the internet only through Traefik on api.omnisurg.app.
name: omnisurg

networks:
  omnisurg_net:
    name: omnisurg_net

services:
  postgres:
    image: postgres:16-alpine
    container_name: omnisurg-postgres
    restart: unless-stopped
    # POSTGRES_USER/PASSWORD/DB and the 12 per-service role passwords come from
    # the rendered postgres env file; init.sh creates one role plus one database
    # per service, each with its own password (one role per service).
    env_file:
      - path: /opt/omnisurg/env/postgres.env
        required: false
    volumes:
      - /opt/omnisurg/data/postgres:/var/lib/postgresql/data
      - /opt/omnisurg/postgres/init.sh:/docker-entrypoint-initdb.d/01-omnisurg-init.sh:ro
    networks: [omnisurg_net]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: omnisurg-redis
    restart: unless-stopped
    # REDIS_PASSWORD comes from the rendered redis env file; the $$ escapes to a
    # single $ so the container shell expands it (compose never sees the secret).
    env_file:
      - path: /opt/omnisurg/env/redis.env
        required: false
    command: ["sh", "-c", "exec redis-server --requirepass \"$$REDIS_PASSWORD\""]
    volumes:
      - /opt/omnisurg/data/redis:/data
    networks: [omnisurg_net]
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 10

  traefik:
    image: traefik:v3.1
    container_name: omnisurg-traefik
    restart: unless-stopped
    command:
      - "--configfile=/etc/traefik/traefik.yml"
    ports:
      - "8080:8080"
      - "8443:8443"
    volumes:
      # No Docker socket is mounted: routing uses the file provider only (one
      # api host to one backend), so Traefik needs no access to the daemon.
      - /opt/omnisurg/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - /opt/omnisurg/traefik/dynamic.yml:/etc/traefik/dynamic.yml:ro
      # The Cloudflare Origin CA cert and key for api.omnisurg.app (Full strict).
      - /opt/omnisurg/tls:/etc/traefik/tls:ro
    networks: [omnisurg_net]
    depends_on:
      admin-bff:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 10s
      timeout: 3s
      start_period: 5s
      retries: 5

  identity-service:
    image: ghcr.io/omnisurg/omnisurg-identity-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-identity-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8081
      OMNISURG_GRPC_PORT: 9081
      OMNISURG_MIGRATIONS_TABLE: identity_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/identity.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/api/v1/identity/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  tenant-service:
    image: ghcr.io/omnisurg/omnisurg-tenant-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-tenant-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8082
      OMNISURG_GRPC_PORT: 9082
      OMNISURG_MIGRATIONS_TABLE: tenant_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/tenant.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8082/api/v1/tenant/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  patient-service:
    image: ghcr.io/omnisurg/omnisurg-patient-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-patient-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8083
      OMNISURG_GRPC_PORT: 9083
      OMNISURG_MIGRATIONS_TABLE: patient_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/patient.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8083/api/v1/patient/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  clinical-service:
    image: ghcr.io/omnisurg/omnisurg-clinical-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-clinical-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8084
      OMNISURG_GRPC_PORT: 9084
      OMNISURG_MIGRATIONS_TABLE: clinical_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/clinical.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8084/api/v1/clinical/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  referral-service:
    image: ghcr.io/omnisurg/omnisurg-referral-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-referral-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8085
      OMNISURG_GRPC_PORT: 9085
      OMNISURG_MIGRATIONS_TABLE: referral_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/referral.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8085/api/v1/referral/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  billing-service:
    image: ghcr.io/omnisurg/omnisurg-billing-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-billing-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8086
      OMNISURG_GRPC_PORT: 9086
      OMNISURG_MIGRATIONS_TABLE: billing_schema_migrations
      # FX hop: billing reaches currency over HTTP by service name.
      OMNISURG_CURRENCY_BASE_URL: http://currency-service:8092
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/billing.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
      currency-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8086/api/v1/billing/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  payment-service:
    image: ghcr.io/omnisurg/omnisurg-payment-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-payment-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8087
      OMNISURG_GRPC_PORT: 9087
      OMNISURG_MIGRATIONS_TABLE: payment_schema_migrations
      # OMNISURG_BILLING_BASE_URL is intentionally UNSET: the optional billing
      # cross-read is OFF in P1 (the BFF supplies invoice context). Mobile money
      # stays a pending shell (HELD): no Paynow wiring here.
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/payment.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8087/api/v1/payment/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  claims-service:
    image: ghcr.io/omnisurg/omnisurg-claims-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-claims-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8088
      OMNISURG_GRPC_PORT: 9088
      OMNISURG_MIGRATIONS_TABLE: claims_schema_migrations
      # Claims reaches billing over HTTP by service name in the claim build path.
      OMNISURG_BILLING_BASE_URL: http://billing-service:8086
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/claims.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
      billing-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8088/api/v1/claims/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  notification-service:
    image: ghcr.io/omnisurg/omnisurg-notification-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-notification-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8089
      OMNISURG_GRPC_PORT: 9089
      OMNISURG_MIGRATIONS_TABLE: notification_schema_migrations
      # The Afrosoft base URL is non-secret env-specific wiring (the real
      # production domain); the API key is a secret and comes from the env file.
      OMNISURG_AFROSOFT_SENDER_ID: OmniSurg
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/notification.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8089/api/v1/notification/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  scheduling-service:
    image: ghcr.io/omnisurg/omnisurg-scheduling-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-scheduling-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8090
      OMNISURG_GRPC_PORT: 9090
      OMNISURG_MIGRATIONS_TABLE: scheduling_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/scheduling.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8090/api/v1/scheduling/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  audit-service:
    image: ghcr.io/omnisurg/omnisurg-audit-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-audit-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8091
      OMNISURG_GRPC_PORT: 9091
      OMNISURG_MIGRATIONS_TABLE: audit_schema_migrations
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/audit.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8091/api/v1/audit/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  currency-service:
    image: ghcr.io/omnisurg/omnisurg-currency-service:${IMAGE_TAG:-prod}
    container_name: omnisurg-currency-service
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8092
      OMNISURG_GRPC_PORT: 9092
      OMNISURG_MIGRATIONS_TABLE: currency_schema_migrations
      # Redis caches the latest rate per pair; address by service name. The Redis
      # password is a secret and comes from the env file. The ZimRate upstream
      # URL is env-specific and also comes from the env file.
      OMNISURG_REDIS_ADDR: redis:6379
      OMNISURG_REDIS_DB: 0
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/currency.env
        required: false
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8092/api/v1/currency/health > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]

  admin-bff:
    image: ghcr.io/omnisurg/omnisurg-admin-bff:${IMAGE_TAG:-prod}
    container_name: omnisurg-admin-bff
    restart: unless-stopped
    environment:
      OMNISURG_ENV: production
      OMNISURG_LOG_LEVEL: info
      OMNISURG_HTTP_PORT: 8093
      OMNISURG_GRPC_PORT: 9093
      # gRPC peers by service name on omnisurg_net. Every target here has a
      # matching depends_on below (HG-B2: no BFF peer may be unwired).
      OMNISURG_GRPC_IDENTITY_TARGET: identity-service:9081
      OMNISURG_GRPC_TENANT_TARGET: tenant-service:9082
      OMNISURG_GRPC_PATIENT_TARGET: patient-service:9083
      OMNISURG_GRPC_CLINICAL_TARGET: clinical-service:9084
      OMNISURG_GRPC_REFERRAL_TARGET: referral-service:9085
      OMNISURG_GRPC_BILLING_TARGET: billing-service:9086
      OMNISURG_GRPC_PAYMENT_TARGET: payment-service:9087
      OMNISURG_GRPC_CLAIMS_TARGET: claims-service:9088
      OMNISURG_GRPC_NOTIFICATION_TARGET: notification-service:9089
      OMNISURG_GRPC_SCHEDULING_TARGET: scheduling-service:9090
      OMNISURG_GRPC_CURRENCY_TARGET: currency-service:9092
      OMNISURG_GRPC_AUDIT_TARGET: audit-service:9091
      # Login passthrough hits identity REST by service name.
      OMNISURG_IDENTITY_BASE_URL: http://identity-service:8081
      # Base for the account activation link the BFF texts + shows in the portal.
      # Production serves staff-web per practice at <subdomain>.omnisurg.app, so
      # the BFF substitutes {subdomain} with the target practice's subdomain.
      OMNISURG_STAFF_WEB_URL_TEMPLATE: https://{subdomain}.omnisurg.app
      # The staff web (per-practice subdomains) and provider portal are served from
      # Cloudflare Pages. The BFF allows the provider admin origin plus the Pages
      # preview origins explicitly, and allows every per-practice staff origin under
      # the domain suffix below via a strict single-label suffix match, so a newly
      # onboarded practice origin works with no BFF redeploy.
      OMNISURG_CORS_ORIGINS: https://admin.omnisurg.app,https://staff-web-886.pages.dev,https://provider-portal-6od.pages.dev
      OMNISURG_TENANT_DOMAIN_SUFFIX: omnisurg.app
    env_file:
      - path: /opt/omnisurg/env/admin-bff.env
        required: false
    depends_on:
      identity-service:
        condition: service_healthy
      tenant-service:
        condition: service_healthy
      patient-service:
        condition: service_healthy
      clinical-service:
        condition: service_healthy
      referral-service:
        condition: service_healthy
      billing-service:
        condition: service_healthy
      payment-service:
        condition: service_healthy
      claims-service:
        condition: service_healthy
      notification-service:
        condition: service_healthy
      scheduling-service:
        condition: service_healthy
      currency-service:
        condition: service_healthy
      audit-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8093/healthz > /dev/null"]
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 6
    networks: [omnisurg_net]
